---
/**
 * Test fixture. Renders every prop it was handed as JSON, so a prop that must
 * NOT reach a widget is visible in the output rather than merely absent from an
 * assertion.
 */
const props = Astro.props;
---

<probe>{JSON.stringify(props)}</probe>
