---
interface Props {
  heading: string;
  body: string;
}

const { heading, body } = Astro.props;
---

<section class="baize-panel">
  <h3 class="baize-title baize-title--size-sm">{heading}</h3>
  <p class="baize-text baize-text--tone-lichen baize-text--size-sm">{body}</p>
</section>
