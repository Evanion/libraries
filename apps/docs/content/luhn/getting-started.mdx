# Getting started

By the end of this page you put a check character on Baize's order number, and
`Luhn.validate` refuses the resulting code with one digit mistyped.

<PageSheet
  difficulty={1}
  read={3}
  hands={5}
  requires={[['Overview', '/luhn']]}
  unlocks={[['Generating and validating', '/luhn/usage']]}
/>

## Before you install luhn

- Node 20 or newer, or any browser.
- An ESM project. A CommonJS project needs Node 20.19, 22.12 or newer, whose
  `require()` loads ES modules. An older `require()` fails with
  `ERR_REQUIRE_ESM`.
- No peer dependencies and no runtime dependencies.

## Install luhn

Pick the line for your package manager.

```bash
npm install @evanion/luhn
```

```bash
yarn add @evanion/luhn
```

```bash
pnpm add @evanion/luhn
```

## The default luhn instance needs no configuration

`Luhn` is the instance built over `'0123456789abcdefghijklmnopqrstuvwxyz'`, and
it folds input to lowercase before looking a character up.
`import { Luhn } from '@evanion/luhn'` is the whole setup.

## Put a check character on an order code with luhn

`Luhn.generate` returns the check character for an order number, and
`Luhn.validate` answers whether a code carrying one holds together:

```ts twoslash file=libs/luhn/README.md region=round-trip

```

`checksum` is `l`, and `order-2026-0042-l` is the code the shop prints on the
receipt. `order-2026-0043-l` is the same code with one digit read back wrong,
and `validate` answers `isValid: false` for it, so the shop's till refuses the
code before the database is asked about it.

The hyphens, including the one before `l`, are not part of what was checked.
Both calls drop every character outside the dictionary and fold uppercase to
lowercase, so `ORDER20260042L` validates against the same `l`.

## Now that luhn checks a code, read back what it returns

[Generating and validating](/luhn/usage) is the next page. It shows the whole
result each call returns, including the `phrase` the check character was
computed over and a count of the characters each call dropped.
