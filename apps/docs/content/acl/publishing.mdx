# Giving my rules to another service

<WorkshopNotice slug="acl">
  <Text as="div" size="sm">
    The examples on this page are regions of `libs/acl/README.md` and run under
    `nx test @evanion/acl`.
  </Text>
</WorkshopNotice>

<PageSheet
  difficulty={4}
  read={7}
  hands={20}
  requires={[['Many services', '/acl/federation']]}
  unlocks={[
    ['A policy from another service', '/acl/adopting'],
    ['The matrix document', '/acl/matrix'],
  ]}
/>

**The concept.** Baize's orders service decides who may refund an order. The
storefront draws the refund button and needs the same answer, and orders hands
over the four permissions that concern the storefront out of the forty it runs
on.

**What you will have.** A JSON document holding the permissions you marked,
whole, which a consumer adopts and evaluates against its own subject, and a
freshness bound that stops it deciding on a copy you have since changed.

**Why you would want it.** Say nothing and the storefront team writes the rules
again from what they can see. That copy diverges the first time orders changes a
rule nobody thinks to propagate, and it diverges silently, because orders is
still refusing correctly on the real matrix.

**How the library gets you there.** Mark a permission `visibility: 'public'`.
`serialize(access, 'reduced')` keeps the marked permissions with every rule they
carry, drops the rest, and trims the schema to the kinds that survived.

`serialize(access, 'reduced')` turns the policy a service already evaluates into
a document its consumers evaluate. Mark each permission `visibility: 'public'`
or leave it unmarked; the reduced form keeps the marked ones and drops the rest.

## Why an acl contract exists

A backend for frontend (BFF) in front of an orders service that cannot read
orders' rules writes its own. That copy is orders' permission logic maintained
by a team that does not own it, and it diverges the first time orders changes a
rule nobody thinks to propagate.

Orders keeps refusing correctly, so nothing is breached and no alert fires. The
refund button is hidden against a server that would have allowed the call, and it
stays hidden, in a UI bug on a different team's backlog.

## Marking and serializing an acl policy

An unmarked permission is internal. A document written before the field existed
publishes nothing, and so does a permission whose author forgot the mark.

```ts file=libs/acl/README.md region=contract

```

```mermaid caption="What a reduced serialization keeps. A permission marked visibility public goes out with every rule it carries, deny rules included; an unmarked permission stays internal, and an object kind left with no public permission loses its schema entry too. The published permissions arrive unmarked, so a consumer cannot publish them on again."
flowchart LR
  M["the matrix orders runs<br>forty permissions"] --> S["serialize(access, 'reduced')"]:::accent
  S -->|"visibility public"| K["the contract<br>four permissions, whole"]
  S -->|"unmarked"| I["stays internal"]
  K --> C["the storefront<br>parseMatrix"]
  C --> R["orders decides again<br>when the refund call arrives"]
```

The consumer holds one contract per origin and evaluates each against its own
subject. Nothing merges them, and the doctrine on
[Many services](/acl/federation) is unchanged: every layer decides for itself,
and orders decides again on its own matrix when the refund call arrives.

## A published permission goes out whole

Drop one of a kept permission's deny rules and you turn a refusal into an allow.
Drop an allow rule or a field config and you move the answer the other way. So a
permission goes out with its refusals attached or it stays internal, and a
permission whose deny rules read an internal concept either discloses that
concept or stays home.

The owner accepts that disclosure, and the owner is the one who decides it. The
deny rule in the example above shows `subject.tier` and the word `probation` to
every consumer.

The contract's decisions equal orders' own, key for key, which is what makes the
contract worth holding. If a contract answered more conservatively, it would be
a second copy again, and a button hidden by caution looks exactly like a button
hidden by a rule.

<Panel heading="A contract is terminal">
  <Text as="div" size="sm">
    A contract's permissions arrive unmarked, so a consumer's own reduced
    serialization comes out empty. That closes the case where one team publishes
    another team's rules on again.
  </Text>
</Panel>

## How long a consumer may keep deciding on an acl contract

`maxStale` is the owner's ceiling, in milliseconds, measured from the consumer's
last successful freshness check. The consumer reports that instant as
`fetchedAt` and may tighten the bound with its own `maxStale`, never extend it.
Past `fetchedAt + min(the two)` every key answers `stale-contract`, and the
remedy is a fetch of the document.

The engine measures from that last check because a consumer whose poll silently
stopped believes it is fresh and starts no clock of its own. Only the owner's
bound expires it. A consumer that reports no `fetchedAt` claims no freshness and
runs under no bound, which is every matrix a service authors in its own process.

A stale contract is stale-permissive, and one thing makes that safe: orders
re-evaluates on its own matrix on every call and refuses. A stale consumer
over-shows and never over-grants.

## A key another team may veto has to be published

`serialize(access, 'reduced', { vetoable })` refuses a listed key the reduction
would otherwise drop. A compliance team checks its own contribution against the
published contract by running
[`applyDenyOverlay`](/acl/federation#a-cross-cutting-deny-is-an-overlay-the-owner-applies),
which merges another team's deny rules into a document before the document is
constructed. An internal vetoable key is absent from that contract, and it takes
its object kind's schema entry along, so the check refuses every contribution it
was written to accept.

## Where to go next

- [Many services](/acl/federation) — the topology this publishes across, and the
  deny overlay a compliance team applies
- [A policy from another service](/acl/adopting) — the same crossing from the
  consumer's side
- [Why was this refused?](/acl/refusals) — what `stale-contract` tells a UI
