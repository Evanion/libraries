# One policy behind a screen

<WorkshopNotice slug="acl">
  <Text as="div" size="sm">
    Every example on this page is a region of `libs/acl/README.md` and runs
    under `nx test @evanion/acl`.
  </Text>
</WorkshopNotice>

<PageSheet
  difficulty={2}
  read={7}
  hands={20}
  requires={[['Advanced setup', '/acl/advanced']]}
  unlocks={[
    ['Platforms', '/acl/platforms'],
    ['Why was this refused?', '/acl/refusals'],
  ]}
/>

**The concept.** Baize's Brass: Birmingham listing has an action bar with three
buttons on it: Review, Edit, Publish. A customer sees one of them, a bookseller
sees two, the owner sees three, and once the listing is published the Edit
button leaves for all three. Nobody writes an `if` to make that happen.

**What you will have.** An action bar that holds a list of actions and no
conditions, and four policy statements that decide what it draws for whoever is
signed in.

**Why you would want it.** Put the rule in the component and you have written
it twice: once here, once in the handler the button posts to. They agree the day
you write them. Then somebody tightens the handler, the button stays lit, and a
bookseller presses Edit on a published listing and gets a 403 with no
explanation.

**How the library gets you there.** The bar calls `access.can` once per action
and draws from `allowed`. `reason` is what the disabled button says. Adding a
fourth control is a statement in the policy and a row in the list.

## The shop's listing policy, in four statements

A listing is a game the shop sells. A customer can review it, a bookseller can
edit it, the owner can publish it, and a published listing is closed to edits
whoever asks:

```ts file=libs/acl/README.md region=listing-bar

```

Four statements, three controls, three roles. The typed builder binds `Listing`
to the key `listing`, so `object.status` in the deny is checked against the
interface as it is written — the same binding [the intermediate
tier](/acl/intermediate) introduced, here carrying an interface rather than a
test.

## What the bar draws for each shopper

The action bar renders one button per action and asks `access.can` for each. A
`Decision` is what decides whether the button is drawn, and `reason` is what
decides what it says when it is drawn and disabled.

For the bookseller above, `review` and `edit` come back `allowed`, `publish`
comes back `no-rule-matched`. Three answers, three controls, and the component
holds no condition of its own — it holds a list of actions.

That is the property worth taking away: adding a fourth control to the bar is a
statement in the policy and a row in the list. It is never a branch in the
component, because a branch in the component is a rule written somewhere the
policy cannot see.

## A status change moves the bar without touching the policy

Press Publish and `object.status` becomes `published`. The deny in the shop's
listing policy now matches, `edit` comes back `denied`, and the Edit button
leaves the bar for every role — the bookseller's and the owner's alike, because
a deny outranks a matching allow.

Nothing was recomputed by hand and no second rule was written for the published
case. One row changed and four statements produced a different screen.

## The screen, with the policy in your hands

Both beats are below, on the policy this page has been reading. Sign in as each
of the three and the bar is rebuilt from `access.can`. Press a role inside a
grant to strike it out and the control that grant produced leaves while you are
looking at it. Press Publish and the deny matches, so Edit goes for the
bookseller and the owner alike.

<AccessDemo />

The policy in the panel is built by the same `policy()` call the region above
runs, and the buttons are `Decision.allowed` and nothing else. This is the
demonstration the front page opens with, mounted here where the four statements
have already been explained.

## The browser's copy of the shop's policy decides nothing

The decisions this screen renders are convenience. The same `can` runs again in
the shop's server runtime, over a subject that runtime resolved itself, before
any listing is edited or published. A screen that hid the button does not excuse
the handler the button posts to. [The security contract](/acl/security) is the
long form, and [Platforms](/acl/platforms) is where the second call sits on each
stack.

## Where a refused control gets its wording

`Decision` carries `reason`, `rule` and `missing` beside `allowed`, and a bar
that greys a control out rather than removing it reads them. What each key
means, and which of them are repairable, is [Why was this
refused?](/acl/refusals); the shape itself is in the [API
reference](/acl/api#decisions).
