# API reference

<WorkshopNotice slug="acl" />

`@evanion/acl` exports the evaluator, the typed builder, the document types and
the construction errors, one heading each below. Node 20 or newer, ESM only.
`@evanion/acl/testing` is a second entry point holding the assertions a
consumer of a published contract writes, and its exports are headed the same
way from [`contractDrift`](#contractdrift) down. The React provider and hooks
are [`@evanion/react-acl`](/react-acl/api).

<ApiFilter />

## `hydratePolicy`

<!-- reference @evanion/acl#hydratePolicy example=matrix-round-trip -->

`hydratePolicy` copies the document, freezes and validates the copy, and throws
on an unknown key. All three type parameters default to the open forms JSON
carries.

## `parseMatrix`

`parseMatrix` is `hydratePolicy` with `closed: true`, for a document another
service published.

```ts signature
function parseMatrix<
  Sub = Subject,
  R = AnyObjects,
  Keys extends string = string,
>(matrix: Matrix, options?: AccessOptions): Access<Sub, R, Keys>;
```

An unknown object kind or action answers `unknown-action` and nothing throws.
`Keys` is the union the consumer expects, unchecked against the document. See
[Adopting a foreign matrix](/acl/matrix#adopting-a-foreign-matrix).

## `policy`

`policy` returns the typed builder, one `.for()` per object kind. See
[Typed authoring](/acl/authoring).

```ts signature
function policy<
  Sub,
  Objects,
  Vocabs extends Partial<Record<keyof Objects, string>> = Record<never, never>,
>(
  options?: PolicyOptions,
): Policy<Sub, Objects, Vocabs, Record<never, never>, never>;
```

| Type parameter | What it is                                                                                                                         |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `Sub`          | the subject type every `subject.*` path is checked against                                                                         |
| `Objects`      | each object kind mapped to its row type                                                                                            |
| `Vocabs`       | each kind mapped to the actions it answers for                                                                                     |
| `Action`       | the vocabulary a kind `Vocabs` omits gets, carrying no wildcard member; `docs/specs/2026-09-17-acl-wildcard-action.md` records why |
| `VocabularyOf` | what one kind's block may declare from; `string` there opens the kind to any action                                                |

## `Policy`

```ts signature
interface Policy<
  Sub,
  Objects,
  Vocabs extends Partial<Record<keyof Objects, string>>,
  R,
  Keys extends string = string,
> {
  for<K extends keyof Objects & string, Act extends string = string>(
    key: K,
    build: (
      p: Actions<Sub, Objects[K], never, VocabularyOf<Vocabs, K>>,
    ) => Actions<Sub, Objects[K], Act, VocabularyOf<Vocabs, K>> | void,
  ): Policy<
    Sub,
    Objects,
    Vocabs,
    R & Record<K, Objects[K]>,
    Keys | PermissionKeys<K, Act>
  >;
  readonly matrix: Readonly<Matrix>;
  build(options?: AccessOptions): Access<Sub, R, Keys>;
}
```

| Member or type   | What it is                                                                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `for`            | takes no type arguments, reading the object type out of `Objects`, the vocabulary out of `Vocabs` and `Act` off the chain its block returns             |
| `matrix`         | flattens the blocks without building one                                                                                                                |
| `build`          | validates every block once and returns the `Access`                                                                                                     |
| `PermissionKeys` | the keys one block contributes; an `Act` the compiler reads as `string` contributes `string`, widening the document's whole key union                   |
| `KeysOf`         | lifts those keys out of an `Access` type; a producer exports `KeysOf<typeof access>` for a consumer compiled against its source                         |
| `ActionOf`       | the action half of the keys one object kind holds, checked against every query's action parameter; open, empty and unmatched key unions answer `string` |

## `PolicyOptions`

```ts signature
type PolicyOptions = Pick<Matrix, 'version' | 'schema'>;
```

Both are document fields, so they land in the flattened JSON.

## `AccessOptions`

```ts signature
interface AccessOptions {
  version?: string | number;
  closed?: boolean;
  fetchedAt?: Instant;
  maxStale?: number;
}
```

| Option      | What it is                                                                                          |
| ----------- | --------------------------------------------------------------------------------------------------- |
| `version`   | overrides the document's own `version`                                                              |
| `closed`    | fails closed on an unknown key                                                                      |
| `fetchedAt` | when this holder last validated; a holder that supplies it obliges the document to state `maxStale` |
| `maxStale`  | a local ceiling, `min`-ed with the document's                                                       |

## `Access`

`Access` is the evaluator over one frozen document.

```ts signature
interface Access<Sub = Subject, R = AnyObjects, Keys extends string = string> {
  readonly matrix: Readonly<Matrix>;
  readonly version: string | number | undefined;
  readonly schema: MatrixSchema | undefined;

  can(subject, key, action, object?, now?): Decision;
  canMany(subject, key, action, objects, now?): Decision[];
  canFields(subject, key, action, object, axis, proposed?, now?): FieldDecision;
  capabilities(subject, now?): Record<Keys, Decision>;
  authorize(subject, options?: { now?: Instant }): Authorized<R, Keys>;
  object(key): BoundKind<Sub, R[K], ActionOf<Keys, K>>;
  readsObject(key, action): boolean;
}
```

| Member                        | What it answers                                             |
| ----------------------------- | ----------------------------------------------------------- |
| `can`                         | one decision for one action on one row                      |
| `canMany`                     | one decision per row, settling one clock for the whole list |
| `canFields`                   | the field-level decision on the `read` or `write` axis      |
| `capabilities`                | every permission key, decided against one subject           |
| `authorize`                   | an `Authorized` handle carrying the subject                 |
| `object`                      | a `BoundKind` handle carrying the object kind               |
| `readsObject`                 | whether the permission needs the row before it can decide   |
| `matrix`, `version`, `schema` | the frozen document, and the two envelope fields off it     |

Every object parameter takes a partial: a projection is the
[`unevaluable`](/acl/refusals#when-the-answer-is-unevaluable) case. An absent
`subject.*` path is a definite miss.

## `Authorized`

`Authorized` is what `authorize(subject)` returns.

```ts signature
interface Authorized<R = AnyObjects, Keys extends string = string> {
  can<K extends keyof R & string>(
    key: K,
    action: ActionOf<Keys, K>,
    object?: Partial<R[K]>,
  ): Decision;
  canMany<K extends keyof R & string>(
    key: K,
    action: ActionOf<Keys, K>,
    objects: readonly Partial<R[K]>[],
  ): Decision[];
  canFields<K extends keyof R & string>(
    key: K,
    action: ActionOf<Keys, K>,
    object: Partial<R[K]>,
    axis: 'read' | 'write',
    proposed?: Partial<R[K]>,
  ): FieldDecision;
  capabilities(): Record<Keys, Decision>;
}
```

`Authorized` omits `matrix`, `version`, `schema` and `readsObject`, which are
not facts about one subject.

## `BoundKind`

`BoundKind` is what `object(key)` returns.

```ts signature
interface BoundKind<Sub, Obj, Act extends string = string> {
  can(
    subject: Sub,
    action: Act,
    object?: Partial<Obj>,
    now?: Instant,
  ): Decision;
  canMany(
    subject: Sub,
    action: Act,
    objects: readonly Partial<Obj>[],
    now?: Instant,
  ): Decision[];
  canFields(
    subject: Sub,
    action: Act,
    object: Partial<Obj>,
    axis: 'read' | 'write',
    proposed?: Partial<Obj>,
    now?: Instant,
  ): FieldDecision;
  readsObject(action: Act): boolean;
}
```

`authorize` binds the other axis; the two compose.

## `KeysOf`

```ts signature
type KeysOf<A> = A extends Access<never, never, infer Keys> ? Keys : never;
```

## `ActionOf`

```ts signature
type ActionOf<Keys extends string, K extends string> = [Keys] extends [never]
  ? string
  : string extends Keys
    ? string
    : [Extract<Keys, `${K}.${string}`>] extends [never]
      ? string
      : Keys extends `${K}.${infer Act}`
        ? Act
        : never;
```

## `CRUD_ACTIONS`

```ts signature
const CRUD_ACTIONS: readonly ['create', 'read', 'update', 'delete'];
```

`CRUD_ACTIONS` declares the four default verbs, and `Action` reads off it. Pass
it to `allowEach`.

## `pickAllowedFields`

`pickAllowedFields` returns the subset of a proposed write the decision marked
`allowed`, and throws `ActionNotAllowedError` on a refused action. See
[the write path](/acl/fields#writing).

```ts signature
function pickAllowedFields<T extends Record<string, unknown>>(
  decision: FieldDecision,
  proposed: T,
): Partial<T>;
```

## `serialize`

```ts signature
function serialize<Sub, R, Keys>(
  access: Access<Sub, R, Keys>,
  mode: 'full',
): Matrix;
function serialize<Sub, R, Keys>(
  access: Access<Sub, R, Keys>,
  mode: 'reduced',
  options?: SerializeOptions<Keys>,
): Matrix;
```

`full` hands back `access.matrix`. `reduced` keeps each permission marked
`visibility: 'public'` whole, strips the marking, and adds the `schema` entries
for the kinds they name. See
[Many services](/acl/federation#publishing-the-rules-to-a-consumer).

## `SerializeMode`

```ts signature
type SerializeMode = 'full' | 'reduced';
```

## `SerializeOptions`

```ts signature
interface SerializeOptions<Keys extends string = string> {
  readonly vetoable?: readonly Keys[];
}
```

A listed key the reduction would drop raises `UnpublishedVetoableError`. The
compiler refuses a key the document never held, where the builder authored the
policy.

## `federatedPolicies`

`federatedPolicies` composes one `Access` per origin and merges no document. See
[Many services](/acl/federation).

```ts signature
function federatedPolicies(
  policies: Readonly<Record<string, Access<Subject, AnyObjects, never>>>,
): FederatedAccess;
```

## `FederatedAccess`

```ts signature
interface FederatedAccess {
  can(
    subject: Subject,
    key: string,
    action: string,
    object?: Record<string, unknown>,
    now?: Instant,
  ): Decision;
  capabilities(subject: Subject, now?: Instant): Record<string, Decision>;
  get(origin: string): Access | undefined;
}
```

`can` routes to the origin holding `` `${object}.${action}` ``; a key no member
holds answers `unknown-action`. `capabilities` settles one instant for all.

## `applyDenyOverlay`

`applyDenyOverlay` appends another team's deny rules to the keys this document
opens for veto. See
[Many services](/acl/federation#a-cross-cutting-deny-is-an-overlay-the-owner-applies).

```ts signature
function applyDenyOverlay(
  matrix: Matrix,
  overlay: DenyOverlay,
  options: DenyOverlayOptions,
): Matrix;
```

## `DenyOverlay`

```ts signature
type DenyOverlay = Readonly<Record<string, readonly Rule[]>>;
```

A `Rule` spells no allow and no field rule, so an overlay only subtracts.

## `DenyOverlayOptions`

```ts signature
interface DenyOverlayOptions {
  readonly vetoable: readonly string[];
}
```

A key listed in `vetoable` obliges the matrix to declare that key's object kind
in `schema.objects`.

## `diffMatrix`

`diffMatrix` compares two matrix documents and reports access rather than text.
See [What did this change do to access?](/acl/changes).

```ts signature
function diffMatrix(before: Matrix, after: Matrix): MatrixDiff;
```

The evaluator ORs the rules on a side, so a side that matched goes on matching
whatever is appended. An added allow branch can therefore only widen and an
added deny branch only narrow. A finding is a direction and an upper bound,
never an exact set.

## `MatrixDiff`

```ts signature
interface MatrixDiff {
  readonly findings: readonly DiffFinding[];
  readonly unchanged: boolean;
  readonly version: {
    readonly before?: string | number;
    readonly after?: string | number;
  };
}
```

`unchanged` is true when the two documents state one policy. Rule order and
condition key order are not policy, so a reordered document reports nothing.

## `DiffFinding`

```ts signature
type DiffFinding =
  GrantedFinding | WithdrawnFinding | UndeterminedFinding | ReadsObjectFinding;
```

Discriminated on `kind`. Every member carries `key`, the permission key the
finding is about.

## `findingsOf`

`findingsOf` returns the findings of one kind, narrowed to that member.

```ts signature
function findingsOf<K extends DiffFinding['kind']>(
  diff: MatrixDiff,
  kind: K,
): readonly Extract<DiffFinding, { kind: K }>[];
```

`MatrixDiff.findings` is one list because a report is read in order. A caller
rendering a section of that report wants one kind, and reaching it otherwise
means a `filter` and a narrowing written the same way at every call site.
The order `diffMatrix` built the report in is kept.

## `GrantedFinding`

```ts signature
interface GrantedFinding {
  readonly kind: 'granted';
  readonly key: string;
  readonly cause: GrantedCause;
  readonly rule?: string;
  readonly when: readonly Condition[];
  readonly groups: ConditionGroups;
  readonly fields?: readonly string[];
}
```

`when` is the added branch whole and `groups` is it split for rendering.
`fields` carries the field names added, where `cause` is `field-list-widened`.

## `GrantedCause`

```ts signature
type GrantedCause =
  | 'permission-added'
  | 'allow-branch-added'
  | 'deny-branch-removed'
  | 'field-list-widened';
```

## `WithdrawnFinding`

```ts signature
interface WithdrawnFinding {
  readonly kind: 'withdrawn';
  readonly key: string;
  readonly cause: WithdrawnCause;
  readonly rule?: string;
  readonly when: readonly Condition[];
  readonly groups: ConditionGroups;
  readonly holders?: { readonly closed: string; readonly open: string };
  readonly fields?: readonly string[];
}
```

`holders` accompanies `permission-removed` and carries a sentence each for a
`parseMatrix` holder and a `hydratePolicy` holder, because one removal answers
`unknown-action` for the first and throws for the second.

## `WithdrawnCause`

```ts signature
type WithdrawnCause =
  | 'permission-removed'
  | 'allow-branch-removed'
  | 'deny-branch-added'
  | 'field-list-narrowed';
```

## `UndeterminedFinding`

```ts signature
interface UndeterminedFinding {
  readonly kind: 'undetermined';
  readonly key: string;
  readonly cause: UndeterminedCause;
  readonly rule?: string;
  readonly before: unknown;
  readonly after: unknown;
}
```

A change `diffMatrix` declines to call a widening or a narrowing, with both
versions shown.

## `UndeterminedCause`

```ts signature
type UndeterminedCause =
  'rule-edited' | 'both-sides-changed' | 'fields-changed';
```

`rule-edited` is a rule that kept an author-supplied `id` and changed its
conditions. `both-sides-changed` is a permission whose allow side and deny side
both moved. `fields-changed` is a field-rule change outside the one decidable
case, which is two literal name lists carrying no `*` and no `!` entry.

## `ReadsObjectFinding`

```ts signature
interface ReadsObjectFinding {
  readonly kind: 'reads-object';
  readonly key: string;
  readonly before: boolean;
  readonly after: boolean;
  readonly paths: {
    readonly before: readonly string[];
    readonly after: readonly string[];
  };
}
```

A change in which `object.*` paths a decision reads. No access moved, and a
caller that decides in front of the fetch gets `unevaluable` where it got
`allow`.

## `ConditionGroups`

```ts signature
interface ConditionGroups {
  readonly subject: readonly Condition[];
  readonly object: readonly Condition[];
  readonly window: readonly Condition[];
}
```

`subject` is who gained or lost the access, `object` is which rows, and
`window` is the `now` conditions. A condition comparing a subject path against
an object path lands in `object`.

## `Matrix`

`Matrix` is a JSON envelope over a flat list of permissions. See
[The matrix document](/acl/matrix).

```ts signature
interface Matrix {
  readonly version?: string | number;
  readonly maxStale?: number; // how long a holder may keep deciding on it
  readonly schema?: MatrixSchema;
  readonly permissions: readonly Permission[];
}
```

| Type           | What it holds                                                                                                                 |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `Permission`   | one `` `${object}.${action}` `` key, its allow rules, its deny rules and its field rules                                      |
| `Rule`         | an id and a list of AND-ed conditions                                                                                         |
| `Condition`    | one comparison; [The matrix document](/acl/matrix#conditions) has the operator table                                          |
| `FieldRules`   | the `fields` list, then one key per field carrying a write-axis config, so a field named `fields` cannot be configured        |
| `FieldConfig`  | `targets` allow-lists the proposed value and `transitions` is a state machine over the current one; see [Fields](/acl/fields) |
| `MatrixSchema` | optional for a producer, binding where present, per kind: a kind `objects` omits is unchecked                                 |
| `ObjectSchema` | a kind's field types, and the `relations` naming the kinds it points at, one hop; no condition compares a relation            |
| `FieldType`    | a flat string any language emits by reflection, where `?` marks a field that may be absent                                    |
| `Instant`      | an ISO 8601 string, epoch milliseconds, or a `Date`; the string and number forms survive JSON                                 |

## `Permission`

```ts signature
interface Permission {
  key: string; // exactly `${object}.${action}`
  object: ObjectKey;
  action: string; // whatever the producer wrote
  rules?: readonly Rule[]; // allows, OR-ed
  denyRules?: readonly Rule[]; // denies, OR-ed; a matched deny wins
  fields?: FieldRules;
  visibility?: 'public' | 'internal'; // absent is internal
}
```

## `Rule`

```ts signature
interface Rule {
  id?: string; // what a decision reports as `rule`; derived from `when` when absent
  when?: readonly Condition[]; // AND-ed
}
```

## `Condition`

```ts signature
type Condition =
  | { field: 'now'; op: 'before' | 'after'; value: Instant }
  | {
      field: string;
      op: 'eq' | 'ne' | 'in' | 'not-in' | 'contains';
      path?: string; // the `eq`/`ne` form only
      value?: unknown;
    };
```

## `FieldRules`

```ts signature
interface FieldRules {
  fields?: readonly string[]; // ['*', '!status'] or ['body', 'title']
  [field: string]: readonly string[] | FieldConfig | undefined;
}
```

## `FieldConfig`

```ts signature
type FieldConfig =
  | { targets: readonly unknown[] }
  | { transitions: Record<string, readonly unknown[]> };
```

## `MatrixSchema`

```ts signature
interface MatrixSchema {
  readonly subject?: ObjectSchema;
  readonly objects?: Readonly<Record<ObjectKey, ObjectSchema>>;
}
```

## `ObjectSchema`

```ts signature
interface ObjectSchema {
  readonly fields?: Readonly<Record<string, FieldType>>;
  readonly relations?: Readonly<Record<string, ObjectKey>>;
}
```

## `FieldType`

```ts signature
type FieldType =
  | BaseFieldType
  | `${BaseFieldType}[]`
  | `${BaseFieldType}?`
  | `${BaseFieldType}[]?`;
```

## `BaseFieldType`

```ts signature
type BaseFieldType = 'string' | 'number' | 'boolean' | 'instant';
```

## `Instant`

```ts signature
type Instant = string | number | Date;
```

## `Decision`

```ts signature
interface Decision {
  key: string;
  allowed: boolean;
  reason: Reason;
  rule?: string;
  missing?: readonly string[];
}
```

| Field     | What it carries                                                          |
| --------- | ------------------------------------------------------------------------ |
| `key`     | the permission key the query asked about                                 |
| `allowed` | the gate                                                                 |
| `reason`  | output only; [Decisions](/acl/decisions) says which reason carries which |
| `rule`    | output only; the id of the rule that decided                             |
| `missing` | output only; the paths that did not read                                 |

## `Reason`

<!-- reference @evanion/acl#Reason -->

## `FieldDecision`

```ts signature
interface FieldDecision {
  allowed: boolean; // the action is allowed AND every decided field is
  action: Decision; // the action alone
  fields: Record<string, FieldState>;
  reasons: Record<string, FieldReason>;
}
```

| Field     | What it carries                                                                                                                                    |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `allowed` | the action is allowed and every decided field is                                                                                                   |
| `action`  | the action-level `Decision` alone                                                                                                                  |
| `fields`  | every field name mapped to a `FieldState`; compare against `'allowed'`, since comparing against `'denied'` treats an unevaluable field as writable |
| `reasons` | every field name mapped to the `FieldReason` that settled it                                                                                       |

## `FieldState`

```ts signature
type FieldState = 'allowed' | 'denied' | 'unevaluable';
```

See [Pitfalls](/acl/pitfalls#never-hand-filter-the-field-map).

## `FieldReason`

```ts signature
type FieldReason =
  | 'allow'
  | 'not-listed'
  | 'targets-failed'
  | 'transition-failed'
  | 'missing-field'
  | 'proposed-required';
```

## `EvaluationContext`

```ts signature
interface EvaluationContext {
  subject: Record<string, unknown>;
  object?: Record<string, unknown>;
  now?: Instant;
}
```

The entry points assemble an `EvaluationContext`, and `now` settles to one epoch
per call.

## `Subject`

```ts signature
type Subject = Record<string, unknown>;
```

## `AnyObjects`

```ts signature
type AnyObjects = Record<string, Record<string, unknown>>;
```

`AnyObjects` is the open key map `Access` defaults to.

## `ObjectKey`

```ts signature
type ObjectKey = string;
```

## `Actions`

`Actions` is the block parameter `.for()` hands you.

```ts signature
interface Actions<
  Sub,
  Obj,
  Act extends string = string,
  Vocab extends string = string,
> extends Ops<Sub, Obj> {
  allow<A extends Vocab>(
    action: A,
    ...conditions: Cond[]
  ): Actions<Sub, Obj, Act | A, Vocab>;
  deny<A extends Vocab>(
    action: A,
    ...conditions: Cond[]
  ): Actions<Sub, Obj, Act | A, Vocab>;
  allowEach<A extends Vocab>(
    actions: readonly A[],
    ...conditions: Cond[]
  ): Actions<Sub, Obj, Act | A, Vocab>;
  denyEach<A extends Vocab>(
    actions: readonly A[],
    ...conditions: Cond[]
  ): Actions<Sub, Obj, Act | A, Vocab>;
  id(id: string): Actions<Sub, Obj, Act, Vocab>;
  fields(rules: readonly string[] | FieldRules): Actions<Sub, Obj, Act, Vocab>;
  visibility(visibility: Visibility): Actions<Sub, Obj, Act, Vocab>;
}
```

| Member or type          | What it does                                                                |
| ----------------------- | --------------------------------------------------------------------------- |
| `allow`, `deny`         | add one rule to the action named                                            |
| `allowEach`, `denyEach` | write one rule across several actions, one ordinary permission each         |
| `id`                    | name the rule the previous `allow` or `deny` wrote, for a decision's `rule` |
| `fields`, `visibility`  | mark the action last declared, and raise `AclConfigError` after a batch     |
| `Vocab`                 | what this kind may declare                                                  |
| `Act`                   | what this chain declared; only `Act` reaches the permission keys            |

## `Action`

```ts signature
type Action = (typeof CRUD_ACTIONS)[number];
```

## `VocabularyOf`

```ts signature
type VocabularyOf<Vocabs, K extends string> = K extends keyof Vocabs
  ? Vocabs[K] extends string
    ? Vocabs[K]
    : Action
  : Action;
```

## `Visibility`

```ts signature
type Visibility = NonNullable<Permission['visibility']>;
```

`Visibility` is the marking `p.visibility()` writes and
`serialize(access, 'reduced')` reads.

## `PermissionKeys`

```ts signature
type PermissionKeys<K extends string, Act extends string> = string extends Act
  ? string
  : `${K}.${Act}`;
```

## `Ops`

`Ops` holds the condition helpers, bound to the subject and object types.

```ts signature
interface Ops<Sub, Obj> {
  eq<A extends string, B>(
    field: Valid<A, Sub, Obj>,
    operand: Operand<B, Sub, Obj>,
  ): Cond;
  ne<A extends string, B>(
    field: Valid<A, Sub, Obj>,
    operand: Operand<B, Sub, Obj>,
  ): Cond;
  in<A extends string>(
    field: Valid<A, Sub, Obj>,
    values: readonly unknown[],
  ): Cond;
  notIn<A extends string>(
    field: Valid<A, Sub, Obj>,
    values: readonly unknown[],
  ): Cond;
  contains<A extends string>(field: Valid<A, Sub, Obj>, value: unknown): Cond;
  before(field: 'now', instant: Instant): Cond;
  after(field: 'now', instant: Instant): Cond;
  and(...conditions: Cond[]): Cond;
  or(...conditions: Cond[]): Cond;
  readonly always: Cond;
}
```

`not-in` is not an identifier, so the helper is `notIn`.

| Type      | What it is                                                                                             |
| --------- | ------------------------------------------------------------------------------------------------------ |
| `Cond`    | an opaque condition tree, which `build()` flattens to a rule per `or` branch                           |
| `Operand` | a comparand shaped like a namespaced path is checked as one, and any other string is a literal         |
| `Paths`   | the `subject.` and `object.` paths one bound type offers                                               |
| `Valid`   | a path those types carry; the failure branch is the parameter type, so the compiler names the bad path |

## `Cond`

```ts signature
interface Cond {
  readonly node: Node;
}
```

## `Operand`

```ts signature
type Operand<B, Sub, Obj> = B extends
  `subject.${string}` | `object.${string}` | 'now'
  ? Valid<B & string, Sub, Obj>
  : B;
```

## `Paths`

```ts signature
type Paths<T, P extends string> = {
  [K in keyof T & string]: `${P}.${K}`;
}[keyof T & string];
```

## `Valid`

```ts signature
type Valid<S extends string, Sub, Obj> = S extends
  Paths<Sub, 'subject'> | Paths<Obj, 'object'> | 'now'
  ? S
  : `unknown path '${S}' on this resource`;
```

## `@evanion/acl/testing`

`@evanion/acl/testing` is the second entry point. `@evanion/acl` re-exports
none of it, so an application importing the engine pulls none of it into its
bundle, and nothing here imports a test framework. See [Testing a contract I
consume](/acl/testing).

```ts twoslash
import {
  assertAllowed,
  assertNoContractDrift,
  fixtureClock,
} from '@evanion/acl/testing';
```

## `contractDrift`

`contractDrift` replays a consumer's own decisions against two versions of one
contract and reports what moved.

```ts signature
function contractDrift<D = never>(
  options: ContractDriftOptions<D>,
): ContractDriftReport<D>;
```

Both documents are adopted with `parseMatrix`, which fails closed, so a removed
key answers `unknown-action` and is reported in `changed`. Nothing throws
partway through the run.

## `assertNoContractDrift`

`assertNoContractDrift` throws a `ContractDriftError` when the producer removed
a published key or a replayed decision changed, and returns the report
otherwise.

```ts signature
function assertNoContractDrift<D = never>(
  options: ContractDriftOptions<D>,
): ContractDriftReport<D>;
```

An added key passes, so a producer that only widened its surface leaves the
consumer's build green.

## `describeContractDrift`

`describeContractDrift` renders a report as a reader sees it, removals first.

```ts signature
function describeContractDrift(report: ContractDriftReport<unknown>): string;
```

A removal is spelled as a broken contract and a changed decision is spelled as
a change, because one report that spelled them alike would leave the consumer
to work out which of the two it is looking at.

## `ContractDriftOptions`

```ts signature
interface ContractDriftOptions<D = never> {
  readonly pinned: Matrix;
  readonly fetched: Matrix;
  readonly cases?: readonly DecisionCase[];
  readonly now?: Instant; // settled once for every case without its own
  readonly diff?: MatrixDiffer<D>;
}
```

`pinned` is the contract this consumer compiled against and `fetched` is the
one the producer publishes now. `contractDrift` settles `now` once, so it
decides a time-windowed permission at one epoch in both documents and never at
two readings of the wall clock.

## `ContractDriftReport`

```ts signature
interface ContractDriftReport<D = never> {
  readonly pinnedVersion: string | number | undefined;
  readonly fetchedVersion: string | number | undefined;
  readonly removed: readonly string[];
  readonly added: readonly string[];
  readonly changed: readonly DecisionChange[];
  readonly diff: D | undefined;
}
```

`removed` holds the permission keys the pinned contract carries and the fetched
one does not, which is a producer breaking a published contract. `added` is not
a break. `diff` is whatever `options.diff` returned.

## `DecisionCase`

```ts signature
interface DecisionCase {
  readonly name?: string; // defaults to `${key}.${action}`
  readonly subject: Subject;
  readonly key: string;
  readonly action: string;
  readonly object?: Record<string, unknown>;
  readonly now?: Instant;
}
```

The three positional arguments of `can` in a bag, plus a name for the report.
`key` is the object kind and `action` the action, so the permission key the
document carries is `` `${key}.${action}` ``.

## `DecisionChange`

```ts signature
interface DecisionChange {
  readonly name: string;
  readonly case: DecisionCase;
  readonly pinned: Decision;
  readonly fetched: Decision;
}
```

Two decisions are the same when `key`, `allowed`, `reason`, `rule` and every
element of `missing` match, so a refusal that started naming a second
unreadable path arrives as a change.

## `MatrixDiffer`

```ts signature
type MatrixDiffer<D> = (pinned: Matrix, fetched: Matrix) => D;
```

The type of `options.diff`, which [`diffMatrix`](#diffmatrix) satisfies.
`contractDrift` reads nothing out of the result and carries it onto
`report.diff`, so the diff's own shape stays the diff's business and arrives
here as `D`.

## `fixtureClock`

`fixtureClock` computes a document's freshness budget the way the engine
computes it and names the two instants that bracket it.

```ts signature
function fixtureClock(
  matrix: Matrix,
  options?: FixtureClockOptions,
): FixtureClock;
```

Every argument is validated as `hydratePolicy` validates it, so a document
stating no `maxStale` raises `MissingFreshnessBudgetError` from `fixtureClock`,
before the test reaches the construction it was about to write.

## `FixtureClockOptions`

```ts signature
interface FixtureClockOptions {
  fetchedAt?: Instant; // defaults to the wall clock, settled once
  maxStale?: number; // a local ceiling, `min`-ed with the document's
}
```

## `FixtureClock`

```ts signature
interface FixtureClock {
  readonly fetchedAt: number;
  readonly budget: number; // min(matrix.maxStale, options.maxStale)
  readonly expiresAt: number; // fetchedAt + budget
  readonly fresh: number; // the last instant every key still decides on
  readonly stale: number; // the first instant that answers stale-contract
  readonly options: AccessOptions;
  at(ms: number): number;
}
```

`options` carries the two numbers the instants were derived from, so the holder
and the test cannot disagree about the budget.

## `assertAllowed`

`assertAllowed` returns the decision when it allows, and throws naming why it
did not.

```ts signature
function assertAllowed(decision: Decision, context?: string): Decision;
```

The decision is returned so a caller chains on it, and `context` is a label
prefixed onto the message.

## `assertRefused`

`assertRefused` returns the decision when it refuses, and throws naming what it
answered.

```ts signature
function assertRefused(
  decision: Decision,
  reason?: Reason,
  context?: string,
): Decision;
```

Naming `reason` asserts which refusal arrived, and a UI treats three of them
differently: `unevaluable` asks for a refetch of the row, `stale-contract` for
a refetch of the document, and `denied` for neither.

## `assertFieldState`

`assertFieldState` returns the field decision when the named field is in
`state`, and throws naming the whole decision when it is not.

```ts signature
function assertFieldState(
  decision: FieldDecision,
  field: string,
  state: FieldState,
  context?: string,
): FieldDecision;
```

A field the decision carries no entry for is reported apart, because
`decision.fields` and `decision.reasons` are keyed by the names the document
declares and an absent name means the test named a field the permission never
had.

## `explainDecision`

`explainDecision` renders one line naming everything a `Decision` carries.

```ts signature
function explainDecision(decision: Decision): string;
```

A failed `expect(decision.allowed).toBe(true)` reports `true !== false` and
names neither the key, the reason, the rule that decided, nor the paths a
refusal is waiting on.

## `explainFieldDecision`

`explainFieldDecision` renders the action decision a field decision hangs off,
then every field beside its state and its reason.

```ts signature
function explainFieldDecision(decision: FieldDecision): string;
```

`FieldDecision.allowed` is true only when the action is allowed and every field
is, so a false one has at least two places to look: `decision.action` and
`decision.fields`.

## `AclAssertionError`

```ts signature
class AclAssertionError extends Error {}
```

Every assertion in `@evanion/acl/testing` throws this class or a subclass of
it. A plain `Error` subclass and no matcher, so vitest, `node:test` and jest
each report a throw from a test body the same way.

## `ContractDriftError`

```ts signature
class ContractDriftError extends AclAssertionError {
  readonly report: ContractDriftReport<unknown>;
}
```

Raised by `assertNoContractDrift` for a removed key or a changed decision, with
the whole report attached and `describeContractDrift` of that report as the
message.

## `AclConfigError`

```ts signature
class AclConfigError extends Error {}
```

`AclConfigError` is the base of every class below. [Typed
authoring](/acl/authoring#construction-errors) tables the same set by authoring
step.

| Error                             | Raised when                                                                                       |
| --------------------------------- | ------------------------------------------------------------------------------------------------- |
| `InvalidMatrixError`              | the envelope carries no `permissions` array, or a `version` of the wrong type                     |
| `InvalidSchemaError`              | the schema's own shape is wrong; `where` locates it inside the document                           |
| `InvalidPermissionError`          | a permission's `object`, `action`, `rules` or `fields` is malformed                               |
| `InvalidRuleError`                | a rule is not an object, or its `when` is not an array                                            |
| `InvalidConditionError`           | a condition's shape, namespace, path depth or operator pairing is unusable                        |
| `KeyMismatchError`                | `key` is not exactly `` `${object}.${action}` ``                                                  |
| `DuplicatePermissionError`        | two permissions share a key                                                                       |
| `DuplicateRuleIdError`            | two rules of one permission carry the same `id`, counting both sides                              |
| `AmbiguousRuleIdError`            | `p.id()` follows an `allow` or `deny` that flattened to other than one rule                       |
| `UnknownFieldError`               | a condition names a field the schema does not declare                                             |
| `FieldTypeMismatchError`          | `contains` runs against a non-array, or a literal is of the wrong type                            |
| `BangInAllowListError`            | a `!name` entry sits inside an allow-list                                                         |
| `DenyWithoutBaselineError`        | a `!name` entry has no `*` baseline to subtract from                                              |
| `TargetsTransitionsConflictError` | one field configures both `targets` and `transitions`                                             |
| `UnknownObjectKeyError`           | a query on the open path names an unknown object kind; `parseMatrix` fails closed                 |
| `UnknownPermissionError`          | a query on the open path names an unknown key; `parseMatrix` answers `unknown-action`             |
| `OriginCollisionError`            | two origins claim one key, which `federatedPolicies` raises while composing                       |
| `UnvetoablePermissionError`       | an overlay names a key the target does not list as `vetoable`                                     |
| `MissingVetoSchemaError`          | a vetoable key's object kind has no `schema.objects` entry                                        |
| `UnpublishedVetoableError`        | `serialize` reduces away a vetoable key the contract would not ship                               |
| `MissingFreshnessBudgetError`     | a holder reports `fetchedAt` against a document stating no `maxStale`                             |
| `InvalidFreshnessError`           | `fetchedAt` is not an instant, or `maxStale` is not a finite, non-negative number of milliseconds |

## `InvalidMatrixError`

```ts signature
class InvalidMatrixError extends AclConfigError {}
```

## `InvalidSchemaError`

```ts signature
class InvalidSchemaError extends AclConfigError {
  readonly where: string; // schema.objects.question.fields.status
}
```

## `InvalidPermissionError`

```ts signature
class InvalidPermissionError extends AclConfigError {
  readonly key: string;
  readonly field: string;
}
```

## `InvalidRuleError`

```ts signature
class InvalidRuleError extends AclConfigError {
  readonly key: string;
  readonly field: string; // rules[2]
}
```

## `InvalidConditionError`

```ts signature
class InvalidConditionError extends AclConfigError {
  readonly key: string;
  readonly field: string;
  readonly where: string;
}
```

## `KeyMismatchError`

```ts signature
class KeyMismatchError extends AclConfigError {
  readonly key: string;
  readonly object: string;
  readonly action: string;
}
```

## `DuplicatePermissionError`

```ts signature
class DuplicatePermissionError extends AclConfigError {
  readonly key: string;
}
```

## `DuplicateRuleIdError`

```ts signature
class DuplicateRuleIdError extends AclConfigError {
  readonly key: string;
  readonly id: string;
}
```

Two rules of one permission carrying the same `id`. `diffMatrix` indexes each
side by rule id, so the second rule under a taken id replaces the first there
and a branch added beside it is reported as no change. The allow side and the
deny side of one permission share one id space. [Naming a
rule](/acl/authoring#naming-a-rule-with-pid) has the example.

## `AmbiguousRuleIdError`

```ts signature
class AmbiguousRuleIdError extends AclConfigError {
  readonly key: string;
  readonly id: string;
  readonly rules: number;
}
```

A `p.id()` following an `allow` or a `deny` that flattened to other than one
rule. `rules` is how many that call wrote, and an `or()` inside it wrote one per
branch.

## `UnknownFieldError`

```ts signature
class UnknownFieldError extends AclConfigError {
  readonly key: string;
  readonly field: string;
  readonly where: string;
}
```

## `FieldTypeMismatchError`

```ts signature
class FieldTypeMismatchError extends AclConfigError {
  readonly key: string;
  readonly field: string;
  readonly where: string;
}
```

## `BangInAllowListError`

```ts signature
class BangInAllowListError extends AclConfigError {}
```

## `DenyWithoutBaselineError`

```ts signature
class DenyWithoutBaselineError extends AclConfigError {}
```

## `TargetsTransitionsConflictError`

<!-- reference @evanion/acl#TargetsTransitionsConflictError -->

## `UnknownObjectKeyError`

```ts signature
class UnknownObjectKeyError extends AclConfigError {
  readonly key: ObjectKey;
}
```

## `UnknownPermissionError`

```ts signature
class UnknownPermissionError extends AclConfigError {
  readonly key: string;
}
```

## `OriginCollisionError`

```ts signature
class OriginCollisionError extends AclConfigError {
  readonly key: string;
  readonly origins: readonly [string, string];
}
```

## `UnvetoablePermissionError`

```ts signature
class UnvetoablePermissionError extends AclConfigError {
  readonly key: string;
}
```

## `MissingVetoSchemaError`

```ts signature
class MissingVetoSchemaError extends AclConfigError {
  readonly key: string;
  readonly object: ObjectKey;
}
```

## `UnpublishedVetoableError`

```ts signature
class UnpublishedVetoableError extends AclConfigError {
  readonly key: string;
}
```

## `MissingFreshnessBudgetError`

```ts signature
class MissingFreshnessBudgetError extends AclConfigError {}
```

## `InvalidFreshnessError`

```ts signature
class InvalidFreshnessError extends AclConfigError {
  readonly field: string;
}
```

## `ActionNotAllowedError`

```ts signature
class ActionNotAllowedError extends Error {
  readonly key: string;
  readonly reason: Reason;
}
```

`ActionNotAllowedError` is the only error the library raises outside
construction. `pickAllowedFields` throws it when the action is refused.

## What is not exported

`@evanion/acl` exports no `decide`, `decideFields` or `evaluateCondition`. Each
answers a fragment and trusts its caller for the rest.
