# Getting started

By the end of this page your own `policy()` answers two questions correctly: it
lets a customer edit their own question and it refuses a stranger.

<PageSheet
  difficulty={1}
  read={4}
  hands={10}
  requires={[['Overview', '/acl']]}
  unlocks={[['Rules about who the user is', '/acl/subject-rules']]}
/>

## Before you start

- Node 20 or newer.
- An ESM project. `@evanion/acl` ships no CommonJS build.
- TypeScript 5.5 or newer, if you want the compiler to check your object kinds
  and actions. The package runs without it.
- A subject your server already resolves, meaning the user object your auth layer
  loaded with every field on it. acl never authenticates anybody; it takes the
  subject you hand it.

You need no API key, no service to run and no environment variable. `policy()`
turns your rules into a JSON document in your own process, the document
[the Overview](/acl) calls the matrix.

## Install acl

<WorkshopNotice slug="acl">
  <Text as="div" size="sm">
    Until it publishes, run this page inside a clone of
    [Evanion/libraries](https://github.com/Evanion/libraries), where
    `@evanion/acl` resolves from source. The example below is a doctest in
    `libs/acl/README.md`, and `npx nx test @evanion/acl` checks every `// ->`
    answer in it. The commands below are what to run once it is on the
    registry.
  </Text>
</WorkshopNotice>

Pick the line for your package manager.

```bash
npm install @evanion/acl
```

```bash
yarn add @evanion/acl
```

```bash
pnpm add @evanion/acl
```

The `@evanion/acl` entry point imports no
framework, and the React provider and hooks are a separate package,
[`@evanion/react-acl`](/react-acl).

## Write the smallest policy that answers something

A policy needs three calls and nothing else.

- `policy<Subject, Objects>()` names two types. `Subject` is the shape of whoever
  is acting, so `{ id: string }` at its smallest. `Objects` maps each object kind
  to the shape of its row, so `{ question: Question }` declares one kind called
  `question` whose rows are `Question`. Add a kind by adding a key.
- `.for(kind, block)` writes one kind's rules. The `kind` is one of the keys you
  just declared in `Objects`.
- `.build()` returns the evaluator. Name it `access`; every page in this section
  calls it that.

`.for()` hands your block one argument, conventionally `p`, and every condition
comes off it. `p.allow(action, ...conditions)` grants an action when every
condition holds. `p.eq(a, b)` compares two values named by path, and a path is a
string rooted at `subject` or at `object`: `p.eq('object.askedBy', 'subject.id')`
reads `askedBy` off the row and `id` off the subject and asks whether they match.

That is the whole minimum configuration. Baize's rule is that the customer who
asked a question may edit it:

```ts twoslash file=libs/acl/README.md region=first-policy

```

Two questions, two answers. The asker gets `true`. The stranger gets `false`, and
`reason` is `no-rule-matched`, which is the engine refusing because nothing in
the document granted the action. You configured no default and got default deny.

If both calls answered the way the comments say, acl is working in your project.

## Two rules for the code around an acl policy

Build the policy once, at module scope. A policy rebuilt per request hands you an
evaluator that answers identically, so the rebuild walks every rule again on
every request and changes no answer.

Gate on `allowed` and nothing else. No part of the library reads `reason` back to
decide anything, so a handler that branches on `reason` is writing a second rule of
its own, next to the one the policy already holds.

## Now that acl answers, write the rest of your rules

[Rules about who the user is](/acl/subject-rules) is the next page. It takes the
one rule you just wrote, adds the deny rule that states an exception to it, and
covers the three answers a policy reading only the subject can give.
