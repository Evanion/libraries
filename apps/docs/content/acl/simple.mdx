# Rules that read the subject

<WorkshopNotice slug="acl">
  <Text as="div" size="sm">
    Inside this repository the package resolves from source like any other
    workspace package, so the example below runs as written.
  </Text>
</WorkshopNotice>

<PageSheet
  difficulty={1}
  read={5}
  hands={15}
  requires={[['Overview', '/acl']]}
  unlocks={[
    ['Rules that read the object', '/acl/intermediate'],
    ['Can this user do this?', '/acl/asking'],
  ]}
/>

**The concept.** Baize keeps a stock report. Any bookseller may read it. Only
the owner may export it. To answer either question you need to know who is
asking, and you need nothing else: no listing is fetched, no question is loaded,
nothing goes to the database first.

**What you get.** A matrix holding both permissions, a server that resolves
the subject once per request, and a call that answers yes or no for any
permission in the document.

**Why you want it.** The two role checks you write today are fine. The
twentieth is the problem. By then four handlers disagree:

- one tests `roles.includes('owner')`
- another tests `user.isOwner`
- a third was copied before the role was renamed
- the export endpoint somebody added last month tests nothing at all

**How the library gets you there.** `policy()` writes the matrix and hands back
an access object over it. `can` answers one question with a decision. `authorize`
binds the subject, so no handler restates it. `capabilities` answers
every question at once, which is how a menu knows what to draw.

## Install acl

One package, and nothing else comes with it:

```bash
npm install @evanion/acl
```

ESM only, Node 20 or newer. No framework reaches the import graph.

## Write the acl policy

With the package installed, write the policy first. Three calls build one:

- `policy<Subject, Objects>()` names the subject type and the object kinds.
- `.for(key, block)` writes one kind's rules.
- `.build()` hands back the evaluator.

A block holds allow rules and a rule holds conditions. The engine ANDs the
conditions, so the rule matches when every condition in it holds. `p` is the
condition helper the block hands you, and this policy needs one of its methods:
`p.contains(path, value)` reads the array at `subject.roles` and asks whether
the role is in it. Every condition below reads the subject, so `capabilities`
answers the whole document at once. The policy and its answers fit in one
block:

```ts file=libs/acl/README.md region=capabilities

```

One policy holds every object kind your app has. Chain a `.for()` per kind and
the report and the listing sit side by side in the same document. You do not
build a policy per resource.

Nothing is allowed without an allow rule that matched. `report.export` carries
one such rule. The bookseller's roles do not satisfy it, so the decision is
`no-rule-matched`. Default deny is the floor of the engine and you configure
nothing to get it.

```mermaid caption="What the engine decides for a permission carrying allow rules only, which is every permission in the policy above. The engine reads those rules against the subject the caller passed. One rule whose conditions all hold answers allow; no such rule answers no-rule-matched, which is the default deny this policy never states."
flowchart TD
  Q["can(subject, 'report', 'export')"] --> R{"an allow rule<br>whose conditions<br>all hold?"}
  R -->|yes| A["allowed: true<br>reason: allow"]
  R -->|no| N["allowed: false<br>reason: no-rule-matched"]:::accent
```

`access.matrix` is what the blocks flattened to: a JSON envelope over a flat
list of permissions, each carrying a `key` of exactly
`` `${object}.${action}` ``. That document is what crosses to a client, where
`hydratePolicy` turns it back into an evaluator.

## Ask acl a question

`capabilities` answered everything at once. When you want one answer, `can`
takes the subject, the object kind (`report`) and the action (`read`), and
returns a decision:

```ts
const decision = access.can(
  { id: 'u1', roles: ['bookseller'] },
  'report',
  'read',
);
// { key: 'report.read', allowed: true, reason: 'allow', rule: '#0' }
```

`allowed` is the answer. Gate on it and nothing else. `reason` is output only,
and a handler that branches on the reason string grants what the engine refused.

## Bind the subject to one acl handle

Every `can` above restated the subject. A server resolves it from the session
once and then asks many questions about it, so `authorize` takes it once, over
the same report-and-listing policy:

```ts file=libs/acl/README.md region=server-authorize

```

The bookseller reads the report and does not export it. Neither call named who
was asking. The handle carries `can`, `canMany`, `canFields` and `capabilities`
with the subject dropped from each signature, which makes the guarded call the
shortest one to write. That is all the library can do about [complete
mediation](/acl/security#complete-mediation).

<Panel heading="This decision is authoritative only where it is made">
  <Text as="div" size="sm">
    The same call in a browser toggles what a user sees and enforces nothing.
    Hiding a button hides the button, not the request the button would have
    sent. Read [the security contract](/acl/security) before this reaches
    production.
  </Text>
</Panel>

## Where subject-only rules stop

You now have a working policy, and every condition in it reads the subject. The
engine draws a real line there, and two things hold on this side of it:

- The server resolves the subject whole before the call, so no field of it is
  ever missing by accident. Every permission answers `true` or `false`, and
  nothing answers [`unevaluable`](/acl/refusals#when-the-answer-is-unevaluable).
- Nothing has to be fetched to ask a question, so `can` takes three arguments
  and [`capabilities`](/acl/capabilities) answers the whole matrix at once.

Now write one rule that reads the object: the customer who asked may edit their
own question. That single rule breaks both bullets at once, and
[Rules that read the object](/acl/intermediate) is what it brings with it.

## Where to go next

- [Rules that read the object](/acl/intermediate). The typed builder, field
  permissions and the guarded save.
- [Can this user do this?](/acl/asking). The decision shape, and asking about a
  list.
- [What can they do at all?](/acl/capabilities). How a menu draws itself from
  one call.
