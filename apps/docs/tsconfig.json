{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "noEmit": true,
    "emitDeclarationOnly": false,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    },
    // The docs app consumes `@evanion/react-widget` the way a published
    // consumer does: the `import`/`types` conditions, which resolve to the
    // package's built `dist/`. tsconfig.base.json sets
    // `customConditions: ["@evanion/source"]`, which would resolve the import to
    // the library's TypeScript source instead; `next build` type-checks with a
    // plain `tsc` that does not build project references, so the source file
    // would be redirected to a declaration output no target emits (TS6305).
    // `nx.json` orders the library's build ahead of this app's.
    "customConditions": [],
    "outDir": "dist",
    "rootDir": ".",
    "ignoreDeprecations": "6.0"
  },
  "include": [
    "../../apps/docs/.next/types/**/*.ts",
    "../../dist/apps/docs/.next/types/**/*.ts",
    "app/**/*.js",
    "app/**/*.jsx",
    "app/**/*.ts",
    "app/**/*.tsx",
    "content/**/*.mdx",
    "content/**/*.md",
    "content/**/*.ts",
    "content/**/*.tsx",
    "content/**/*.js",
    "content/**/*.jsx",
    "components/**/*.js",
    "components/**/*.jsx",
    "components/**/*.ts",
    "components/**/*.tsx",
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "mdx-components.js"
  ],
  "exclude": [
    "out-tsc",
    "dist",
    "node_modules",
    "jest.config.ts",
    "app/**/*.spec.ts",
    "app/**/*.test.ts",
    ".next",
    "eslint.config.js",
    "eslint.config.cjs",
    "eslint.config.mjs"
  ],
  "references": [
    {
      "path": "../../libs/baize-ui"
    },
    {
      "path": "../../libs/widget"
    }
  ]
}
