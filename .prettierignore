# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
# Regenerated by `next build` on every run -- formatting it just gets reverted
**/next-env.d.ts
# React Router writes its bundle to <app>/build and its route types to
# <app>/.react-router, both on every build. Formatting generated output just
# gets reverted by the next run.
**/build
**/.react-router
# Written by `nx run baize-ui:generate-tokens` from libs/baize-ui/src/tokens/,
# and compared byte for byte against that generator by a test. Prettier wraps a
# long box-shadow value onto a second line, which makes the formatter a second
# producer of the file and fails the comparison.
libs/baize-ui/src/tokens.generated.css
# Written by `nx release` for every package it versions. Its renderer emits two
# spaces after the breaking-change marker and before the commit link, and
# prettier collapses both to one, which makes the formatter a second producer of
# the file.
**/CHANGELOG.md
